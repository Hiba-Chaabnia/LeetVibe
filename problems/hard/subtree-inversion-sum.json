{
  "questionId": "3858",
  "title": "Subtree Inversion Sum",
  "titleSlug": "subtree-inversion-sum",
  "difficulty": "Hard",
  "topicTags": [
    {
      "name": "Array",
      "slug": "array"
    },
    {
      "name": "Dynamic Programming",
      "slug": "dynamic-programming"
    },
    {
      "name": "Tree",
      "slug": "tree"
    },
    {
      "name": "Depth-First Search",
      "slug": "depth-first-search"
    }
  ],
  "content": "You are given an undirected tree rooted at node 0, with n nodes numbered from 0 to n - 1. The tree is represented by a 2D integer array edges of length n - 1, where edges[i] = [ui, vi] indicates an edge between nodes ui and vi.\n\nYou are also given an integer array nums of length n, where nums[i] represents the value at node i, and an integer k.\n\nYou may perform inversion operations on a subset of nodes subject to the following rules:\n\n•\n\tSubtree Inversion Operation:\n\n•\n\t\tWhen you invert a node, every value in the subtree rooted at that node is multiplied by -1.\n\n•\n\tDistance Constraint on Inversions:\n\n•\n\t\tYou may only invert a node if it is \"sufficiently far\" from any other inverted node.\n\n•\n\t\tSpecifically, if you invert two nodes a and b such that one is an ancestor of the other (i.e., if LCA(a, b) = a or LCA(a, b) = b), then the distance (the number of edges on the unique path between them) must be at least k.\n\nReturn the maximum possible sum of the tree's node values after applying inversion operations.\n\nExample 1:\n\nInput: edges = [[0,1],[0,2],[1,3],[1,4],[2,5],[2,6]], nums = [4,-8,-6,3,7,-2,5], k = 2\n\nOutput: 27\n\nExplanation:\n\n• Apply inversion operations at nodes 0, 3, 4 and 6.\n\n• The final nums array is [-4, 8, 6, 3, 7, 2, 5], and the total sum is 27.\n\nExample 2:\n\nInput: edges = [[0,1],[1,2],[2,3],[3,4]], nums = [-1,3,-2,4,-5], k = 2\n\nOutput: 9\n\nExplanation:\n\n• Apply the inversion operation at node 4.\n\n• The final nums array becomes [-1, 3, -2, 4, 5], and the total sum is 9.\n\nExample 3:\n\nInput: edges = [[0,1],[0,2]], nums = [0,-1,-2], k = 3\n\nOutput: 3\n\nExplanation: Apply inversion operations at nodes 1 and 2.\n\nConstraints:\n\n• 2 <= n <= 5 * 104\n\n• edges.length == n - 1\n\n• edges[i] = [ui, vi]\n\n• 0 <= ui, vi < n\n\n• nums.length == n\n\n• -5 * 104 <= nums[i] <= 5 * 104\n\n• 1 <= k <= 50\n\n• The input is generated such that edges represents a valid tree.",
  "codeSnippets": [
    {
      "lang": "Python",
      "langSlug": "python",
      "code": "class Solution(object):\n    def subtreeInversionSum(self, edges, nums, k):\n        \"\"\"\n        :type edges: List[List[int]]\n        :type nums: List[int]\n        :type k: int\n        :rtype: int\n        \"\"\"\n        "
    }
  ],
  "hints": [
    "Use tree‑based dynamic programming",
    "Define your DP state as dp[node][parityFromAncestorInversions][distSinceLastInversion]",
    "node is the current tree node",
    "parityFromAncestorInversions indicates whether the subtree values have been flipped an even (0) or odd (1) number of times by ancestor inversions",
    "distSinceLastInversion tracks the number of edges from this node up to the most recent ancestor inversion"
  ],
  "testCases": [
    [
      "[[0,1],[0,2],[1,3],[1,4],[2,5],[2,6]]",
      "[4,-8,-6,3,7,-2,5]",
      "2"
    ],
    [
      "[[0,1],[1,2],[2,3],[3,4]]",
      "[-1,3,-2,4,-5]",
      "2"
    ],
    [
      "[[0,1],[0,2]]",
      "[0,-1,-2]",
      "3"
    ]
  ],
  "expectedOutputs": [
    "27",
    "9",
    "3"
  ],
  "has_solutions": true,
  "solutions": {
    "python": "from collections import defaultdict\n\nclass Solution(object):\n    def subtreeInversionSum(self, edges, nums, k):\n        n = len(nums)\n        adj = defaultdict(list)\n        for u, v in edges:\n            adj[u].append(v)\n            adj[v].append(u)\n        # dp[parity][dist] = max sum achievable in subtree\n        # parity: 0=even, 1=odd flips from ancestors\n        # dist: distance from most recent ancestor inversion (capped at k)\n        # Iterative DFS with memoization\n        INF_DIST = k  # represents \"last inversion >= k edges ago\"\n        from functools import lru_cache\n        sys_limit = n + 10\n        import sys\n        sys.setrecursionlimit(sys_limit * 100)\n        # Build parent/children structure\n        parent = [-1] * n\n        children = [[] for _ in range(n)]\n        order = []\n        visited = [False] * n\n        stack = [0]\n        visited[0] = True\n        while stack:\n            u = stack.pop()\n            order.append(u)\n            for v in adj[u]:\n                if not visited[v]:\n                    visited[v] = True\n                    parent[v] = u\n                    children[u].append(v)\n                    stack.append(v)\n        # Bottom-up DP\n        # dp[u][parity][dist] = max sum of subtree of u\n        # Since k <= 50, dist in [1..k]\n        dp = [[[-float('inf')] * (k + 1) for _ in range(2)] for _ in range(n)]\n        for u in reversed(order):\n            for parity in range(2):\n                for dist in range(1, k + 1):\n                    val = nums[u] if parity == 0 else -nums[u]\n                    # Option 1: don't invert at u\n                    child_sum = 0\n                    new_dist = min(dist + 1, k)\n                    for c in children[u]:\n                        child_sum += dp[c][parity][new_dist]\n                    no_inv = val + child_sum\n                    # Option 2: invert at u (only if dist >= k)\n                    if dist >= k:\n                        val2 = -nums[u] if parity == 0 else nums[u]\n                        child_sum2 = 0\n                        for c in children[u]:\n                            child_sum2 += dp[c][1 - parity][1]  # children are distance 1 from inversion\n                        inv = val2 + child_sum2\n                        dp[u][parity][dist] = max(no_inv, inv)\n                    else:\n                        dp[u][parity][dist] = no_inv\n        return dp[0][0][k]  # root, no prior inversions (dist = k = \"no recent inversion\")",
    "explanation": "Tree DP bottom-up. State: (node, parity, dist) where parity=0/1 = even/odd ancestor inversions, dist = distance from nearest ancestor inversion (capped at k meaning 'no recent inversion'). At each node: don't invert (propagate parity, dist+1 capped at k) or invert if dist≥k (flip parity, children get dist=1). n ≤ 50000, k ≤ 50, so O(n*2*k) = O(5*10^6).\n\nTime: O(n * k). Space: O(n * k)."
  },
  "similarQuestions": "[]"
}