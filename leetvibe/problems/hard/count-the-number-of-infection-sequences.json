{
  "questionId": "3224",
  "title": "Count the Number of Infection Sequences",
  "titleSlug": "count-the-number-of-infection-sequences",
  "difficulty": "Hard",
  "topicTags": [
    {
      "name": "Array",
      "slug": "array"
    },
    {
      "name": "Math",
      "slug": "math"
    },
    {
      "name": "Combinatorics",
      "slug": "combinatorics"
    }
  ],
  "content": "You are given an integer n and an array sick sorted in increasing order, representing positions of infected people in a line of n people.\n\nAt each step, one uninfected person adjacent to an infected person gets infected. This process continues until everyone is infected.\n\nAn infection sequence is the order in which uninfected people become infected, excluding those initially infected.\n\nReturn the number of different infection sequences possible, modulo 109+7.\n\nExample 1:\n\nInput: n = 5, sick = [0,4]\n\nOutput: 4\n\nExplanation: There is a total of 6 different sequences overall.\n\n• Valid infection sequences are [1,2,3], [1,3,2], [3,2,1] and [3,1,2].\n\n• [2,3,1] and [2,1,3] are not valid infection sequences because the person at index 2 cannot be infected at the first step.\n\nExample 2:\n\nInput: n = 4, sick = [1]\n\nOutput: 3\n\nExplanation: There is a total of 6 different sequences overall.\n\n• Valid infection sequences are [0,2,3], [2,0,3] and [2,3,0].\n\n• [3,2,0], [3,0,2], and [0,3,2] are not valid infection sequences because the infection starts at the person at index 1, then the order of infection is 2, then 3, and hence 3 cannot be infected earlier than 2.\n\nConstraints:\n\n• 2 <= n <= 105\n\n• 1 <= sick.length <= n - 1\n\n• 0 <= sick[i] <= n - 1\n\n• sick is sorted in increasing order.",
  "codeSnippets": [
    {
      "lang": "Python",
      "langSlug": "python",
      "code": "class Solution(object):\n    def numberOfSequence(self, n, sick):\n        \"\"\"\n        :type n: int\n        :type sick: List[int]\n        :rtype: int\n        \"\"\"\n        "
    }
  ],
  "hints": [
    "Consider infected children as 0 and non-infected as 1, then divide the array into segments with the same value.",
    "For each segment of non-infected children whose indices are [i, j] and indices (i - 1) and (j + 1), if they exist, are already infected. Then if i == 0 or j == n - 1, each second there is only one kid that can be infected (which is at the other endpoint).",
    "If i > 0 and j < n - 1, we have two choices per second since the children at the two endpoints can both be the infect candidates. So there are 2<sup>j - i</sup> orders to infect all children in the segment.",
    "Each second we can select a segment and select one endpoint from it.",
    "The answer is: \r\nS! / (len[1]! * len[2]! * ... * len[m]! * len<sub>start</sub>! * len<sub>end</sub>!) * 2<sup>k</sup> \r\nwhere len[1], len[2], ..., len[m] are the lengths of each segment of non-infected children that have an infected child at both endpoints, len<sub>start</sub> and len<sub>end</sub> denote the number of non-infected children with infected child at one endpoint, S is the total length of all segments of non-infected children, and k = (len[1] - 1) + (len[2] - 1) + ... + (len[m] - 1)."
  ],
  "testCases": [
    [
      "5",
      "[0,4]"
    ],
    [
      "4",
      "[1]"
    ]
  ],
  "expectedOutputs": [
    "4",
    "3"
  ],
  "has_solutions": true,
  "solutions": {
    "python": "class Solution(object):\n    def numberOfSequence(self, n, sick):\n        \"\"\"\n        :type n: int\n        :type sick: List[int]\n        :rtype: int\n        \"\"\"\n        MOD = 10**9 + 7\n        \n        # Compute factorials\n        fact = [1] * n\n        for i in range(1, n):\n            fact[i] = fact[i - 1] * i % MOD\n        \n        m = len(sick)\n        S = n - m  # Total uninfected\n        \n        result = fact[S]\n        \n        # Segments of uninfected\n        segments = []\n        \n        # Before first sick\n        if sick[0] > 0:\n            segments.append((sick[0], 1))  # (length, type: 1 if only left endpoint)\n        \n        # Between sick people\n        for i in range(len(sick) - 1):\n            gap = sick[i + 1] - sick[i] - 1\n            if gap > 0:\n                segments.append((gap, 2))  # (length, type: 2 if both endpoints)\n        \n        # After last sick\n        if sick[-1] < n - 1:\n            segments.append((n - 1 - sick[-1], 1))\n        \n        # Calculate\n        two_count = 0\n        for length, seg_type in segments:\n            if seg_type == 1:\n                # Only one endpoint infected\n                result = result * pow(fact[length], MOD - 2, MOD) % MOD\n            else:\n                # Both endpoints infected - can choose 2^(length-1) orderings\n                two_count += length - 1\n                result = result * pow(fact[length], MOD - 2, MOD) % MOD\n        \n        # Multiply by 2^k where k = total (length-1) for double-sided segments\n        result = result * pow(2, two_count, MOD) % MOD\n        \n        return result",
    "explanation": "Infection sequence combinatorics:\n\n1. **Segments of Uninfected**:\n   - Before first sick\n   - Between consecutive sick people\n   - After last sick\n\n2. **Segment Types**:\n   - One endpoint: can only infect from that side\n   - Both endpoints: can choose which end to infect each second\n\n3. **Counting Formula**:\n   - S! / (len[1]! * len[2]! * ... * len[m]!) * 2^k\n   - Where k = sum of (length - 1) for two-endpoint segments\n   - Represents choosing which endpoint to infect at each step\n\nTime: O(m)\nSpace: O(n)"
  },
  "similarQuestions": "[{\"title\": \"Contain Virus\", \"titleSlug\": \"contain-virus\", \"difficulty\": \"Hard\"}, {\"title\": \"Amount of Time for Binary Tree to Be Infected\", \"titleSlug\": \"amount-of-time-for-binary-tree-to-be-infected\", \"difficulty\": \"Medium\"}]"
}