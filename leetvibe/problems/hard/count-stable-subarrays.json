{
  "questionId": "4110",
  "title": "Count Stable Subarrays",
  "titleSlug": "count-stable-subarrays",
  "difficulty": "Hard",
  "topicTags": [
    {
      "name": "Array",
      "slug": "array"
    },
    {
      "name": "Binary Search",
      "slug": "binary-search"
    },
    {
      "name": "Prefix Sum",
      "slug": "prefix-sum"
    }
  ],
  "content": "You are given an integer array nums.\n\nA subarray of nums is called stable if it contains no inversions, i.e., there is no pair of indices i < j such that nums[i] > nums[j].\n\nYou are also given a 2D integer array queries of length q, where each queries[i] = [li, ri] represents a query. For each query [li, ri], compute the number of stable subarrays that lie entirely within the segment nums[li..ri].\n\nReturn an integer array ans of length q, where ans[i] is the answer to the ith query.​​​​​​​​​​​​​​\n\nNote:\n\n• A single element subarray is considered stable.\n\nExample 1:\n\nInput: nums = [3,1,2], queries = [[0,1],[1,2],[0,2]]\n\nOutput: [2,3,4]\n\nExplanation:​​​​​\n\n• For queries[0] = [0, 1], the subarray is [nums[0], nums[1]] = [3, 1].\n\n• The stable subarrays are [3] and [1]. The total number of stable subarrays is 2.\n\n• For queries[1] = [1, 2], the subarray is [nums[1], nums[2]] = [1, 2].\n\n• The stable subarrays are [1], [2], and [1, 2]. The total number of stable subarrays is 3.\n\n• For queries[2] = [0, 2], the subarray is [nums[0], nums[1], nums[2]] = [3, 1, 2].\n\n• The stable subarrays are [3], [1], [2], and [1, 2]. The total number of stable subarrays is 4.\n\nThus, ans = [2, 3, 4].\n\nExample 2:\n\nInput: nums = [2,2], queries = [[0,1],[0,0]]\n\nOutput: [3,1]\n\nExplanation:\n\n• For queries[0] = [0, 1], the subarray is [nums[0], nums[1]] = [2, 2].\n\n• The stable subarrays are [2], [2], and [2, 2]. The total number of stable subarrays is 3.\n\n• For queries[1] = [0, 0], the subarray is [nums[0]] = [2].\n\n• The stable subarray is [2]. The total number of stable subarrays is 1.\n\nThus, ans = [3, 1].\n\nConstraints:\n\n• 1 <= nums.length <= 105\n\n• 1 <= nums[i] <= 105\n\n• 1 <= queries.length <= 105\n\n• queries[i] = [li, ri]\n\n• 0 <= li <= ri <= nums.length - 1",
  "codeSnippets": [
    {
      "lang": "Python",
      "langSlug": "python",
      "code": "class Solution(object):\n    def countStableSubarrays(self, nums, queries):\n        \"\"\"\n        :type nums: List[int]\n        :type queries: List[List[int]]\n        :rtype: List[int]\n        \"\"\"\n        "
    }
  ],
  "hints": [
    "Identify maximal non-decreasing segments. Each segment of length L contributes L * (L + 1) / 2 stable subarrays.",
    "Build a prefix array of total stable subarrays ending at each index.",
    "For query [l, r], compute the prefix sum in the range and adjust for the left segment crossing l."
  ],
  "testCases": [
    [
      "[3,1,2]",
      "[[0,1],[1,2],[0,2]]"
    ],
    [
      "[2,2]",
      "[[0,1],[0,0]]"
    ]
  ],
  "expectedOutputs": [
    "[2,3,4]",
    "[3,1]"
  ],
  "has_solutions": true,
  "solutions": {
    "python": "class Solution(object):\n    def countStableSubarrays(self, nums, queries):\n        \"\"\"\n        :type nums: List[int]\n        :type queries: List[List[int]]\n        :rtype: List[int]\n        \"\"\"\n        n = len(nums)\n        \n        # Find maximal non-decreasing segments\n        # Segment length L contributes L*(L+1)/2 stable subarrays\n        \n        prefix = [0] * (n + 1)  # prefix[i] = total stable subarrays in nums[0..i-1]\n        \n        i = 0\n        while i < n:\n            j = i\n            # Find maximal non-decreasing segment\n            while j < n - 1 and nums[j] <= nums[j + 1]:\n                j += 1\n            \n            # Segment [i, j] of length (j - i + 1)\n            seg_len = j - i + 1\n            seg_count = seg_len * (seg_len + 1) // 2\n            \n            # Update prefix for all positions in segment\n            for k in range(i + 1, j + 2):\n                prefix[k] = prefix[k - 1] + (k - i) * ((k - i) + 1) // 2\n            \n            i = j + 1\n        \n        # Process queries\n        result = []\n        for l, r in queries:\n            # Count stable subarrays in [l, r]\n            # Use prefix sums\n            \n            total = 0\n            i = l\n            while i <= r:\n                j = i\n                # Find maximal non-decreasing segment in [l, r]\n                while j < r and nums[j] <= nums[j + 1]:\n                    j += 1\n                \n                # Segment [i, j] contributes\n                seg_len = j - i + 1\n                seg_count = seg_len * (seg_len + 1) // 2\n                total += seg_count\n                \n                i = j + 1\n            \n            result.append(total)\n        \n        return result",
    "explanation": "Stable subarray counting with segments:\n\n1. **Stable = Non-Decreasing**: nums[i] <= nums[i+1]\n2. **Maximal Segments**: Find non-decreasing runs\n3. **Contribution**: Segment of length L gives L*(L+1)/2 subarrays\n4. **For Query [l,r]**:\n   - Find non-decreasing segments within range\n   - Sum their contributions\n\nTime: O(n + q*n)\nSpace: O(n)"
  },
  "similarQuestions": "[]"
}